'use strict';
const teacherApplicationService = require('../services/TeacherApplicationService');

const { TeacherValidator, TeacherMapper } = require('../dto');

class TeacherController {
  async post_upload_practical(req, res) {
    try {
      // Replaced by Zod Validator
      const result = await teacherApplicationService.post_upload_practical(data);
      if (!result) return;
      if (result._isSend) return res.status(result._status ?? 200).send(result._body);
      return res.status(result._status ?? 200).json(result._body);
    } catch (err) {
      const status = err.status || err.statusCode || (err.code === 'VALIDATION_ERROR' ? 400 : 500);
      return res.status(status).json({ success: false, message: err.message || 'Lỗi server', errors: err.errors });
    }
  }

  async post_root(req, res) {
    try {
      const data = {
        body: req.body, query: req.query, params: req.params,
        userBranchId: req.user?.branchId, userBranchCode: req.user?.branchCode,
        app: req.app, skipSideEffects: req.body?.skipSideEffects
      };
      const result = await teacherApplicationService.post_root(data);
      if (!result) return;
      if (result._isSend) return res.status(result._status ?? 200).send(result._body);
      return res.status(result._status ?? 200).json(result._body);
    } catch (err) {
      const status = err.status || err.statusCode || (err.code === 'VALIDATION_ERROR' ? 400 : 500);
      return res.status(status).json({ success: false, message: err.message || 'Lỗi server', errors: err.errors });
    }
  }

  async get_root(req, res) {
    try {
      // Replaced by Zod Validator
      const result = await teacherApplicationService.get_root(data);
      if (!result) return;
      if (result._isSend) return res.status(result._status ?? 200).send(result._body);
      return res.status(result._status ?? 200).json(result._body);
    } catch (err) {
      const status = err.status || err.statusCode || (err.code === 'VALIDATION_ERROR' ? 400 : 500);
      return res.status(status).json({ success: false, message: err.message || 'Lỗi server', errors: err.errors });
    }
  }

  async get_stats_summary(req, res) {
    try {
      // Replaced by Zod Validator
      const result = await teacherApplicationService.get_stats_summary(data);
      if (!result) return;
      if (result._isSend) return res.status(result._status ?? 200).send(result._body);
      return res.status(result._status ?? 200).json(result._body);
    } catch (err) {
      const status = err.status || err.statusCode || (err.code === 'VALIDATION_ERROR' ? 400 : 500);
      return res.status(status).json({ success: false, message: err.message || 'Lỗi server', errors: err.errors });
    }
  }

  async get_id(req, res) {
    try {
      // Replaced by Zod Validator
      const result = await teacherApplicationService.get_id(TeacherValidator.validateGet_id(req));
      if (!result) return;
      if (result._isSend) return res.status(result._status ?? 200).send(result._body);
      return res.status(result._status ?? 200).json(result._body);
    } catch (err) {
      const status = err.status || err.statusCode || (err.code === 'VALIDATION_ERROR' ? 400 : 500);
      return res.status(status).json({ success: false, message: err.message || 'Lỗi server', errors: err.errors });
    }
  }

  async put_id(req, res) {
    try {
      // Replaced by Zod Validator
      const result = await teacherApplicationService.put_id(TeacherValidator.validatePut_id(req));
      if (!result) return;
      if (result._isSend) return res.status(result._status ?? 200).send(result._body);
      return res.status(result._status ?? 200).json(result._body);
    } catch (err) {
      const status = err.status || err.statusCode || (err.code === 'VALIDATION_ERROR' ? 400 : 500);
      return res.status(status).json({ success: false, message: err.message || 'Lỗi server', errors: err.errors });
    }
  }

  async put_id_score(req, res) {
    try {
      // Replaced by Zod Validator
      const result = await teacherApplicationService.put_id_score(TeacherValidator.validatePut_id_score(req));
      if (!result) return;
      if (result._isSend) return res.status(result._status ?? 200).send(result._body);
      return res.status(result._status ?? 200).json(result._body);
    } catch (err) {
      const status = err.status || err.statusCode || (err.code === 'VALIDATION_ERROR' ? 400 : 500);
      return res.status(status).json({ success: false, message: err.message || 'Lỗi server', errors: err.errors });
    }
  }

  async put_id_approve(req, res) {
    try {
      // Replaced by Zod Validator
      const result = await teacherApplicationService.put_id_approve(TeacherValidator.validatePut_id_approve(req));
      if (!result) return;
      if (result._isSend) return res.status(result._status ?? 200).send(result._body);
      return res.status(result._status ?? 200).json(result._body);
    } catch (err) {
      const status = err.status || err.statusCode || (err.code === 'VALIDATION_ERROR' ? 400 : 500);
      return res.status(status).json({ success: false, message: err.message || 'Lỗi server', errors: err.errors });
    }
  }

  async post_id_submit_practical(req, res) {
    try {
      // Replaced by Zod Validator
      const result = await teacherApplicationService.post_id_submit_practical(TeacherValidator.validatePost_id_submit_practical(req));
      if (!result) return;
      if (result._isSend) return res.status(result._status ?? 200).send(result._body);
      return res.status(result._status ?? 200).json(result._body);
    } catch (err) {
      const status = err.status || err.statusCode || (err.code === 'VALIDATION_ERROR' ? 400 : 500);
      return res.status(status).json({ success: false, message: err.message || 'Lỗi server', errors: err.errors });
    }
  }

  async put_id_reject(req, res) {
    try {
      // Replaced by Zod Validator
      const result = await teacherApplicationService.put_id_reject(TeacherValidator.validatePut_id_reject(req));
      if (!result) return;
      if (result._isSend) return res.status(result._status ?? 200).send(result._body);
      return res.status(result._status ?? 200).json(result._body);
    } catch (err) {
      const status = err.status || err.statusCode || (err.code === 'VALIDATION_ERROR' ? 400 : 500);
      return res.status(status).json({ success: false, message: err.message || 'Lỗi server', errors: err.errors });
    }
  }

  async delete_id(req, res) {
    try {
      // Replaced by Zod Validator
      const result = await teacherApplicationService.delete_id(TeacherValidator.validateDelete_id(req));
      if (!result) return;
      if (result._isSend) return res.status(result._status ?? 200).send(result._body);
      return res.status(result._status ?? 200).json(result._body);
    } catch (err) {
      const status = err.status || err.statusCode || (err.code === 'VALIDATION_ERROR' ? 400 : 500);
      return res.status(status).json({ success: false, message: err.message || 'Lỗi server', errors: err.errors });
    }
  }

  async get_id_finance(req, res) {
    try {
      // Replaced by Zod Validator
      const result = await teacherApplicationService.get_id_finance(TeacherValidator.validateGet_id_finance(req));
      if (!result) return;
      if (result._isSend) return res.status(result._status ?? 200).send(result._body);
      return res.status(result._status ?? 200).json(result._body);
    } catch (err) {
      const status = err.status || err.statusCode || (err.code === 'VALIDATION_ERROR' ? 400 : 500);
      return res.status(status).json({ success: false, message: err.message || 'Lỗi server', errors: err.errors });
    }
  }

  async get_id_finance_pending(req, res) {
    try {
      // Replaced by Zod Validator
      const result = await teacherApplicationService.get_id_finance_pending(TeacherValidator.validateGet_id_finance_pending(req));
      if (!result) return;
      if (result._isSend) return res.status(result._status ?? 200).send(result._body);
      return res.status(result._status ?? 200).json(result._body);
    } catch (err) {
      const status = err.status || err.statusCode || (err.code === 'VALIDATION_ERROR' ? 400 : 500);
      return res.status(status).json({ success: false, message: err.message || 'Lỗi server', errors: err.errors });
    }
  }

  async put_id_finance_pay_flexible(req, res) {
    try {
      // Replaced by Zod Validator
      const result = await teacherApplicationService.put_id_finance_pay_flexible(TeacherValidator.validatePut_id_finance_pay_flexible(req));
      if (!result) return;
      if (result._isSend) return res.status(result._status ?? 200).send(result._body);
      return res.status(result._status ?? 200).json(result._body);
    } catch (err) {
      const status = err.status || err.statusCode || (err.code === 'VALIDATION_ERROR' ? 400 : 500);
      return res.status(status).json({ success: false, message: err.message || 'Lỗi server', errors: err.errors });
    }
  }

  async put_id_finance_pay_all(req, res) {
    try {
      // Replaced by Zod Validator
      const result = await teacherApplicationService.put_id_finance_pay_all(TeacherValidator.validatePut_id_finance_pay_all(req));
      if (!result) return;
      if (result._isSend) return res.status(result._status ?? 200).send(result._body);
      return res.status(result._status ?? 200).json(result._body);
    } catch (err) {
      const status = err.status || err.statusCode || (err.code === 'VALIDATION_ERROR' ? 400 : 500);
      return res.status(status).json({ success: false, message: err.message || 'Lỗi server', errors: err.errors });
    }
  }

}

module.exports = new TeacherController();
