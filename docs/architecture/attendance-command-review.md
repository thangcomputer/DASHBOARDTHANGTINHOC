# Attendance Domain Command Review
## Overview
Write operations for `Attendance` dispatch via `CommandBus`. This ensures that batch check-ins publish specific `DomainEvents` to trigger analytics updates asynchronously.
